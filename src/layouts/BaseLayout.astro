---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const {
  title = 'Neadev — Soluciones a problemas reales, implementadas con software',
  description = 'Resolvemos problemas de negocio con software a medida. Automatización, integración y productos digitales con equipo senior y entregas predecibles.',
  ogImage = '/og-image.png',
} = Astro.props;
---

<!doctype html>
<html lang="es" data-i18n-lang>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title data-i18n-html="seo.title">{title}</title>
    <meta name="description" content={description} data-i18n-html="seo.description" />
    <meta name="robots" content="index, follow" />
    <link rel="canonical" href="https://neadev.dev/" />
    <meta name="theme-color" content="#0C0D0E" media="(prefers-color-scheme: dark)" />
    <meta name="theme-color" content="#F0F1F3" media="(prefers-color-scheme: light)" />

    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://neadev.dev/" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={ogImage} />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />

    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={ogImage} />

    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />

    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:opsz,wght@14..32,400..600&display=swap" rel="stylesheet" media="print" onload="this.media='all'" />
    <noscript><link href="https://fonts.googleapis.com/css2?family=Instrument+Serif:ital@0;1&family=Inter:opsz,wght@14..32,400..600&display=swap" rel="stylesheet" /></noscript>

    <script type="application/ld+json" id="json-ld" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "Organization",
      "name": "Neadev",
      "url": "https://neadev.dev",
      "logo": "https://neadev.dev/favicon.svg",
      "description": "Resolvemos problemas de negocio con software a medida. Automatización, integración y productos digitales con equipo senior y entregas predecibles.",
      "foundingDate": "2024",
      "contactPoint": {
        "@type": "ContactPoint",
        "contactType": "sales",
        "email": "hello@neadev.dev"
      },
      "sameAs": [
        "https://github.com/neadev",
        "https://linkedin.com/company/neadev"
      ]
    })} />

    <script is:inline>
      (function() {
        document.documentElement.classList.add('js');
        var stored = localStorage.getItem('theme');
        if (stored === 'light') {
          document.documentElement.classList.add('light');
        }
      })();
    </script>

    <script src="/src/scripts/i18n.js" defer></script>
    <script src="/src/scripts/gsap.js" type="module" defer></script>
  </head>
  <body class="antialiased">
    <div data-bg-grid class="bg-grid" aria-hidden="true" />
    <a href="#main-content" class="skip-link">Saltar al contenido principal</a>
    <slot />
  </body>
</html>
